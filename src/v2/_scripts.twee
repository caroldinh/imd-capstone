:: UserScript[script]
if (!window.renderUnderwater) {
    window.renderUnderwater = () => {

    }
}

document.getElementById("story").addEventListener( 'DOMNodeInserted', function ( event ) {
   $(this)[0].scrollIntoView({ behavior: "smooth", block: "end", inline: "end" });
}, false );
